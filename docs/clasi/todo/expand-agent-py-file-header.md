---
status: pending
---

# Expand File Header on agent.py

The module docstring for `clasi/agent.py` is minimal:

```
Agent class hierarchy for CLASI.
```

Expand it to describe the class hierarchy (Agent, MainController,
DomainController, TaskWorker), the dispatch lifecycle, and how
agents relate to contracts and templates.
