---
id: '002'
title: Fix project-initiation output directory to docs/clasi/design/
status: done
use-cases:
- SUC-002
depends-on: []
github-issue: ''
todo: project-init-docs-in-design-dir.md
---
<!-- CLASI: Before changing code or making plans, review the SE process in CLAUDE.md -->

# Fix project-initiation output directory to docs/clasi/design/

## Description

The `project-initiation` skill should always direct output (overview.md,
specification.md, usecases.md) to `docs/clasi/design/`. However, the overview has
sometimes been generated at the wrong path. This ticket audits and fixes all
locations where the output directory is specified.

Files to audit:
- `plugin/skills/project-initiation/SKILL.md` (source; after ticket 001 this will be
  at `clasi/plugin/skills/project-initiation/SKILL.md`)
- `.claude/skills/project-initiation/SKILL.md` (installed copy — regenerated by
  `clasi init`, so fixing the source fixes the install)
- Any related internal agent definitions that mention the output directory

## Acceptance Criteria

- [x] `plugin/skills/project-initiation/SKILL.md` (or `clasi/plugin/...` after 001)
      explicitly states output goes to `docs/clasi/design/` for all three documents
- [x] No other path (repo root, `docs/clasi/`, etc.) is mentioned as an output
      location in any project-initiation skill or agent instruction
- [x] The `create_overview` MCP tool step is confirmed present and documented as
      required after writing `overview.md`
- [x] After running `clasi init`, the installed `.claude/skills/project-initiation/
      SKILL.md` reflects the corrected output directory
- [x] `uv run pytest` passes

## Implementation Plan

### Approach

1. Read `plugin/skills/project-initiation/SKILL.md` (current location; adjust if
   ticket 001 has already run and it is now at `clasi/plugin/`).
2. Verify whether the output directory is already correctly stated as
   `docs/clasi/design/`. The current version already states this correctly.
3. If any deviation exists, edit to make `docs/clasi/design/` explicit in the
   file header, process steps, and output section.
4. Check `clasi/agents/domain-controllers/sprint-planner/plan-sprint.md` and any
   other agent definition that references the project-initiation output path — update
   if needed.
5. Run `clasi init` in a scratch directory and verify the installed skill file
   matches the source.

### Files to Modify

- `plugin/skills/project-initiation/SKILL.md` (or `clasi/plugin/skills/...` if
  ticket 001 completed first) — confirm/fix output directory statement
- Any internal agent md files that mention project-initiation output paths

### Files to Create

None (this is a verification + fix ticket).

### Testing Plan

- `uv run pytest` for regressions.
- Manual review: read the installed `.claude/skills/project-initiation/SKILL.md`
  and confirm `docs/clasi/design/` appears in the Process section and Output section.

### Documentation Updates

None required beyond the skill file changes themselves.
