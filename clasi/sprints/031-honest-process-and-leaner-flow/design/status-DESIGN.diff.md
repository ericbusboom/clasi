---
source_file: status-DESIGN.md
source_hash: f310ddc62383801e929b733993f8cf416a9d478d152bf04b90eb20416952b098
---
# Diff: status-DESIGN.md

Comparison of the sprint overlay copy of `status-DESIGN.md` against its pristine (seed-commit) canonical version.

```diff
--- status-DESIGN.md (pristine)
+++ status-DESIGN.md (current)
@@ -12,7 +12,7 @@
 
 Five modules, roughly a pipeline from raw state to scoped output:
 
-- `reader.py` — `ClasiStateReader`, the production implementation of `clasi.state_machine.context.StateReader`. Reads real data from three sources (filesystem path/markdown checks, `git` via subprocess, and `StateDB`/SQLite) and returns safe defaults on any failure rather than raising. As of sprint 026, its git-subprocess-backed methods (`git_branch`, `default_branch`, `branch_merged`, etc.) memoize their result per `ClasiStateReader` instance — a single status-inject hook invocation, which evaluates the same git query many times across the project/sprint/ticket machines, now shells out once per distinct query instead of once per predicate (about 28 calls collapsed to about 3 for a typical invocation). The cache is instance-scoped and lives only as long as the hook process; there is no cross-invocation cache to invalidate. As of sprint 027, the 2-3 real `subprocess.run` calls that survive that memoization (`git branch --show-current`, `git symbolic-ref refs/remotes/origin/HEAD`, `git branch --merged <default>`) are further collapsed — each spawn costs about 20-30ms of OS process-creation overhead that no amount of in-process caching removes, identified by ticket 007 (sprint 026) as the dominant remaining cost once sprint-count-scaling was fixed. The exact technique (direct `.git/HEAD`/ref reads, a single batched plumbing call, or both) is a ticket-time, measurement-driven decision rather than a locked-in mechanism — see sprint 027's Design Rationale. `StateReader`'s public method signatures are unchanged; this is an internal implementation change only.
+- `reader.py` — `ClasiStateReader`, the production implementation of `clasi.state_machine.context.StateReader`. Reads real data from three sources (filesystem path/markdown checks, `git` via subprocess, and `StateDB`/SQLite) and returns safe defaults on any failure rather than raising. As of sprint 026, its git-subprocess-backed methods (`git_branch`, `default_branch`, `branch_merged`, etc.) memoize their result per `ClasiStateReader` instance — a single status-inject hook invocation, which evaluates the same git query many times across the project/sprint/ticket machines, now shells out once per distinct query instead of once per predicate (about 28 calls collapsed to about 3 for a typical invocation). The cache is instance-scoped and lives only as long as the hook process; there is no cross-invocation cache to invalidate. As of sprint 027, the 2-3 real `subprocess.run` calls that survive that memoization (`git branch --show-current`, `git symbolic-ref refs/remotes/origin/HEAD`, `git branch --merged <default>`) are further collapsed — each spawn costs about 20-30ms of OS process-creation overhead that no amount of in-process caching removes, identified by ticket 007 (sprint 026) as the dominant remaining cost once sprint-count-scaling was fixed. The exact technique (direct `.git/HEAD`/ref reads, a single batched plumbing call, or both) is a ticket-time, measurement-driven decision rather than a locked-in mechanism — see sprint 027's Design Rationale. `StateReader`'s public method signatures are unchanged; this is an internal implementation change only. **As of sprint 031**: `branch_merged()` and its `is_branch_merged` predicate registration are deleted (see `state_machine-DESIGN.md`'s sprint-031 entry) — it was never actually reached on the status-inject hot path in practice (that path only evaluates the *active* sprint's state, never an archived one, and `is_sprint_archived` — checked first in `closed`'s invariant list per the 030/002 regression fix — is `False` for every active sprint, so `all()` always short-circuited before `branch_merged()` would run), so deleting it cannot regress the `TestGitSpawnCollapseInRealRepo` tests; it only closes the theoretical gap of a future direct `evaluate_state` call against an archived sprint id ever spawning git for this reason.
 - `reporter.py` — `StatusReporter` evaluates the project/sprint/ticket machines against a `StateReader` and assembles the full nested status dict (project state, per-sprint state and tickets, available transitions with `blocked_by` lists, issue counts). Its `exclude_done`-gated sprint sweep (`_build_sprints_block`, used only by the status-inject hook path) skips a sprint's own machine evaluation and its tickets' entirely for any sprint `_is_terminal_sprint` classifies as archived — as of sprint 026 ticket 007, that check matches declared `status: done` **or** `status: closed` (the sprint machine's own terminal state name, distinct from the ticket machine's `done`) and, independently, physical location under the project's `sprints/done/` archive directory regardless of what `status:` says. Before this widening, sprints archived with `status: closed` (the majority of this project's own archive) leaked past the `exclude_done` filter and were fully re-evaluated on every status-inject invocation — 7 sprints instead of 1, about 137 `get_sprint()` / 1,816 `read_frontmatter()` calls per prompt in this repo. **As of sprint 030**: `_is_terminal_sprint`'s dual check (declared-status match OR directory location) is exactly the pattern the sprint-level drift-check redesign in `inconsistency.py` (above) now reuses for its own terminal-sprint exemption — both modules independently arrived at "physical location under `sprints/done/` is the reliable signal, tolerate whatever legacy status string is on disk," and sprint 030 makes that convergence explicit rather than leaving two similar-but-separately-maintained checks. `_build_project_block`/`_build_sprint_block`/`_build_tickets_block` no longer call `_last_matching_state_from_error` on an `AmbiguousStateError` — `evaluate_state` itself now returns a determinate state (see `state_machine-DESIGN.md`'s sprint-030 entry), so the three `except AmbiguousStateError: state_name = _last_matching_state_from_error(...)` blocks in this file are deleted along with the function they called; a genuinely unmatched context still falls back to `"unknown"` via `NoMatchingStateError`, unchanged.
 - `inconsistency.py` — detects state drift. **As of sprint 030**, this module's own comparison is redesigned, not just its callers: before this sprint, the *only* sprint-level check compared frontmatter `status:` (historically 3 values: `roadmap`/`planning-docs`/`closed`) against the computed sprint-machine state name (7 *different* values: `open`/`planned`/`pre-flight`/`ticketed`/`executing`/`review`/`closed`) — two vocabularies that shared only the string `"closed"` by construction, so a healthy active sprint was flagged as drift essentially always (finding 2 of the reliability review's state-layer report). As of sprint 030, sprint-stage drift-checking compares DB phase against frontmatter `status:` instead — the same 8-value vocabulary on both sides, since `Sprint.set_sprint_stage()` (`sprint.py`, see root `DESIGN.md`'s sprint-030 entry) is now the sole writer of both. This is a like-with-like comparison for the first time; a mismatch is a genuine writer bug, not an expected, permanent state. A sprint physically located under `sprints/done/` is exempt from this check entirely, regardless of which legacy `status:` string it carries (`"done"` or `"closed"` — both exist across this repo's 29 archived sprints) — no historical file needed editing to make this exemption correct. The *computed* sprint-machine vocabulary (`open`/`planned`/…) is no longer compared against frontmatter at all — it answers a genuinely different question ("what can happen next," not "what stage is recorded"), and comparing the two was always a category error, not a drift signal; see root `DESIGN.md`'s sprint-030 Constraints entry. Ticket-level drift-checking (frontmatter `status:` vs. the ticket machine's computed state) is unchanged by this sprint. This module still reports each mismatch as a `state_drift` entry naming why, unchanged in shape. As of sprint 026, the `status-inject` hook path (`UserPromptSubmit`, fired on every prompt) does not call this module — running the full drift-detection pass on every prompt cost about 400ms for no per-prompt benefit. `clasi status` (the CLI command) and the `project-status` skill still call it unchanged; drift detection remains a deliberate, on-demand action, not something the hot hook path pays for by default.
 - `narrowing.py` — `narrow_status(full_dict, agent, ...)` filters the full team-lead-scoped dict down to what a `sprint-planner` or `programmer` agent is allowed to see (team-lead: unchanged; sprint-planner: one sprint, no per-ticket detail; programmer: one ticket, summarized parent sprint), with an explicit `notes.fallback` when the narrowing agent didn't supply the ID needed to narrow precisely.
```
