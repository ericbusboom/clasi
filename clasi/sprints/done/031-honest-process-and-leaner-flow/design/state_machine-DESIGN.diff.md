---
source_file: state_machine-DESIGN.md
source_hash: 5f41501a73fece0da0e3f1e7725ae55dc4e96ad9fdf9708b87c9bcf2870b7818
---
# Diff: state_machine-DESIGN.md

Comparison of the sprint overlay copy of `state_machine-DESIGN.md` against its pristine (seed-commit) canonical version.

```diff
--- state_machine-DESIGN.md (pristine)
+++ state_machine-DESIGN.md (current)
@@ -17,7 +17,7 @@
 - `context.py` — the `StateReader` protocol (read-only interface predicates use to reach filesystem/git/DB state without doing I/O themselves) plus per-artifact-type context dataclasses, and `NullStateReader` for tests.
 - `loader.py` — the only module that parses state-machine YAML; loads a named machine (`"project"`, `"sprint"`, or `"ticket"`) from `clasi/schemas/state-machines/<name>.yaml` via `importlib.resources`. As of sprint 026, `load_machine` is wrapped with `functools.lru_cache`: the three packaged machine definitions never change within a process's lifetime, so a single status-inject invocation that evaluates all three machines (project, sprint, ticket) — and, across a sprint with several tickets, re-evaluates the sprint/ticket machines per artifact — now parses each YAML file once per process instead of once per `load_machine` call (about 20 re-parses collapse to 3, one per machine name).
 - `evaluator.py` — the computational core: `evaluate_state` (which state matches the current context), `inspect_transitions` (which outbound transitions from a state are fireable), and `evaluate_predicates` (evaluates a predicate list, capturing per-predicate exceptions rather than propagating them). **As of sprint 030**: `evaluate_state` no longer requires exactly one state's invariants to hold. It defines most-advanced-match-wins — when more than one state matches (declaration order in the machine YAML is significant: later-declared states are "more advanced"), it returns the last match rather than raising `AmbiguousStateError`; zero matches still raises `NoMatchingStateError`, unchanged. This is a real contract change, not a bug fix to the old contract: the sprint machine's `open` and `planned` states have byte-identical invariant lists by design (a checklist machine, not a mutually-exclusive one — see `docs/design/state-machines.md`, which needs a follow-up edit to match, flagged in sprint 030's `sprint.md` Open Questions), so "exactly one match" was never actually achievable and the previous behavior (`AmbiguousStateError` → `status/reporter.py` regexing the exception's own message text for the state-name list, then `ast.literal_eval`-ing it) was a workaround baked into the *caller*, not a real invariant enforced by the *engine*. `_last_matching_state_from_error` and its three call sites in `reporter.py` are deleted.
-- `predicates/project.py`, `sprint.py`, `ticket.py` — the concrete predicate functions registered per artifact type. **As of sprint 030**: every predicate referenced anywhere in the three packaged machines is satisfiable by something the toolchain actually writes (see Constraints below) — `is_any_sprint_ticketed`'s query string, gate-result predicates' semantics, and five predicates with no writer for their backing signal are all corrected or removed; see this doc's `## 3` and the root `DESIGN.md`'s sprint-030 entry.
+- `predicates/project.py`, `sprint.py`, `ticket.py` — the concrete predicate functions registered per artifact type. **As of sprint 030**: every predicate referenced anywhere in the three packaged machines is satisfiable by something the toolchain actually writes (see Constraints below) — `is_any_sprint_ticketed`'s query string, gate-result predicates' semantics, and five predicates with no writer for their backing signal are all corrected or removed; see this doc's `## 3` and the root `DESIGN.md`'s sprint-030 entry. **As of sprint 031**: `is_branch_merged` (`predicates/sprint.py`) is deleted, along with its `ClasiStateReader.branch_merged()` backing method (see `status-DESIGN.md`'s sprint-031 entry) — it referenced something `close_sprint` itself destroys (the sprint branch, deleted after merge), making the sprint machine's `closed` state unsatisfiable by construction for a correctly closed sprint. `sprint.yaml`'s `closed` state invariants drop to `[is_sprint_archived]` alone — the same rule sprint 030's Constraints entry already states (a predicate whose backing signal nothing satisfiable produces is a defect, not an acceptable permanent-False), applied to the third and last reason `closed` could never hold (030 fixed the first two: an unrecordable `sprint_review` gate, then the writer-less `is_close_report_present`/`is_review_satisfied` flags).
 
 ## 3. Constraints and Invariants
 
```
