# Tickets — solo-process

Create implementation tickets under `docs/clasi/sprints/<id>/tickets/`. Each
ticket should have a clear description, acceptance criteria, and an
implementation plan sized for one sitting.

Sequence tickets so dependencies are explicit. No review gate is required
before moving to execution.
