# Close — solo-process

Close the sprint by verifying all tickets are done and the test suite passes,
then write `docs/clasi/sprints/<id>/close-report.md` summarising what was
built and any follow-on TODOs.

Merge the sprint branch and tag the release. No post-close stakeholder sign-off
is required.
